machine:
  node:
    version: $(cat $HOME/$CIRCLE_PROJECT_REPONAME/.nvmrc)

dependencies:
  override:
    - yarn install

test:
  override:
    - yarn test

deployment:
  release:
    branch: master
    commands:
      - DEBUG=condition yarn semantic-release || true
