machine:
  node:
    version: $(cat $HOME/$CIRCLE_PROJECT_REPONAME/.nvmrc)

dependencies:
  override:
    - yarn install

test:
  override:
    - yarn test
  post:
    - if [ $CIRCLE_BRANCH = 'master' ]; then yarn semantic-release; fi
